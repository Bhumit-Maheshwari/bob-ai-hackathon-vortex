# Environment Variables — ChainGuard AI Backend

# Port the Express server listens on.
# The Vite dev-server proxies /api → http://localhost:5000, so keep this at 5000
# during local development unless you also change vite.config.js.
PORT=5000

# Node environment
NODE_ENV=development
